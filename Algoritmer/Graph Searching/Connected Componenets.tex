Et Connected Component er det maksimale undserset af forbundede hjørner\\
\textbf{Pseudokode}:
\begin{lstlisting}[frame=single, mathescape=true]
Connected(G)
	while G has unmarked node u
		BFS(u)
\end{lstlisting}
\textbf{Running time}: O(n + m)
