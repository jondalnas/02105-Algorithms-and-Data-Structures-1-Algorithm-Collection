For alle knuder i faldende niveaue, brug BubbleDown\\
\textbf{Pseaudokode}:
\begin{lstlisting}[frame=single, mathescape=true]
Build2(H)
	for each node in H decreasing v
		BubbleDown(v)
\end{lstlisting}
\textbf{Running Time}: O(n)