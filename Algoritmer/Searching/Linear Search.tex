Gå lineært igennem list og se om x forkommer\\
\textbf{Pseudokode:}
\begin{lstlisting}[frame=single, mathescape=true]
LinearSearch(A, x, n)
	for i = 0 to n
		if A[i] = x return true
	return false
\end{lstlisting}
\textbf{Running time:} O(n)
