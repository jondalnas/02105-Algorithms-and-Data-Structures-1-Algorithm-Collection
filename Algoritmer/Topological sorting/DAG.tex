Check om G er en Directed Acyclic Graph (DAG), ved at chekce om den kan topologisk sorteres\\
\textbf{Pseudokode}:
\begin{lstlisting}[frame=single, mathescape=true]
DAG(G)
	return TopSort(G)
\end{lstlisting}
\textbf{Running time}: O(TopSort)